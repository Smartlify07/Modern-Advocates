import { user, session, account, verification, userRelations, sessionRelations, accountRelations } from "./auth"
import {
  profiles,
  categories,
  courses,
  courseModules,
  courseTopics,
  reviews,
  enrollments,
  courseCategories,
  topicCompletions,
} from "./course"

export const schema = {
  user,
  session,
  account,
  verification,
  userRelations,
  sessionRelations,
  accountRelations,
  profiles,
  categories,
  courses,
  courseModules,
  courseTopics,
  reviews,
  enrollments,
  courseCategories,
  topicCompletions,
}
